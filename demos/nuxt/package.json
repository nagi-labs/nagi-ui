{
  "name": "nagi-ui-nuxt-demo",
  "private": true,
  "description": "CHARTER §10 Phase 0 Demo A: the DropdownMenu blueprint under Nuxt lazy hydration — the native popover opens before (or without) hydration.",
  "type": "module",
  "scripts": {
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint . --ignore-path .gitignore",
    "lint:fix": "oxlint . --ignore-path .gitignore --fix",
    "typecheck": "nuxi typecheck",
    "check": "oxfmt --check && oxlint . --ignore-path .gitignore && nuxi typecheck",
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "prepare": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.16.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^5.9.3",
    "vue": "^3.5.0",
    "vue-tsc": "^3.3.8"
  },
  "packageManager": "pnpm@11.1.3"
}
